apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
kind: OpenStackCluster
metadata:
  name: "${CLUSTER_NAME}"
spec:
  apiServer:
    managedLoadBalancer:
      monitor:
        delay: 15
        timeout: 10
        maxRetries: 3
        maxRetriesDown: 2
