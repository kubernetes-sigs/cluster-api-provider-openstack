---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: openstackservergroups.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: OpenStackServerGroup
    listKind: OpenStackServerGroupList
    plural: openstackservergroups
    singular: openstackservergroup
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: OpenStackServerGroup is the Schema for the openstackservergroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenStackServerGroupSpec defines the desired state of OpenStackServerGroup.
            properties:
              identityRef:
                description: IdentityRef is a reference to a identity to be used when
                  reconciling this resource
                properties:
                  cloudName:
                    description: CloudName specifies the name of the entry in the
                      clouds.yaml file to use.
                    type: string
                  name:
                    description: |-
                      Name is the name of a secret in the same namespace as the resource being provisioned.
                      The secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file.
                      The secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
                    type: string
                required:
                - cloudName
                - name
                type: object
              policy:
                description: Policy is a string with some valid values; affinity,
                  anti-affinity, soft-affinity, soft-anti-affinity.
                type: string
            required:
            - policy
            type: object
          status:
            description: OpenStackServerGroupStatus defines the observed state of
              OpenStackServerGroup.
            properties:
              ready:
                description: Ready is true when the resource is created.
                type: boolean
              uuid:
                description: UUID of provisioned ServerGroup
                type: string
            required:
            - uuid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
